# nginx.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    name: prometheus-resource
    namespace: argocd
spec:
    project: default
    source:
        repoURL: 'https://github.com/MohamedElSayed215/gitops.git'
        path: manifests/prometheus
        targetRevision: main
    destination:
        server: 'https://kubernetes.default.svc'
        namespace: monitoring
    syncPolicy:
      automated:
        selfHeal: true
        prune: true
